Program the (input,output);
Var
   h,z,i,x,y : integer;
   w : array [1..5] of real;

function gcd (a, b: integer) : result real;
begin
   if b= 0 then gcd := a
   else gcd := gcd(b,a mod b)
end

procedure this (why, note : real);
begin
   if (why = note - 1608) or not (note = why)
   then if (x - y = 0)
      then begin
         x := why / note
         end
end

procedure that;
var h,z : real;
begin
   x := y;
   this (h,z)
end

begin
   While (I <= 5) and (x = 75) do
   begin
      w [i] := i + 1;
      x := w[i] * 20.5
      end;
   read (x,y);
   write (gcd(x, y));
   gcd (h,z);
   w[gcd(x,y)] := 23e10;
   this (w,z);
   this (w[w[i]],gcd(x,y));
   that
end.
